.globl main
main:
    pushq   %rbp
    movq    %rsp, %rbp
    movq $9, %rax
    movq %rax, -8(%rbp)
    .L0: 
    cmp $10, -8(%rbp)
    je .L1
    movq -8(%rbp), %rax
    addq $1, %rax
    movq %rax, -16(%rbp)
    movq -16(%rbp), %rax
    movq %rax, -8(%rbp)
    jmp .L0
    .L1:
    movq    $0, %rax
    leave
    ret
