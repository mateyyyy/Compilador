
sumar:
    pushq   %rbp
    movq    %rsp, %rbp
    movq x, %rax
    leave
    ret
    movq    $0, %rax
    leave
    ret
.globl main
main:
    pushq   %rbp
    movq    %rsp, %rbp
    movq $9, %rax
    movq %rax, -8(%rbp)
    movq $1, %rax
    movq %rax, -16(%rbp)
    movq -8(%rbp), %rdi
    movq -16(%rbp), %rsi
    call sumar
    movq %rax, -24(%rbp)
    movq    $0, %rax
    leave
    ret
