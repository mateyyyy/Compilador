.globl main
main:
    pushq   %rbp
    movq    %rsp, %rbp
    movq $5, %rax
    movq %rax, -8(%rbp)
    cmpq $3, -8(%rbp)
    je .L0
    movq $0, %rax
    movq %rax, -16(%rbp)
    movq $5, %rax
    movq %rax, -16(%rbp)
    .L0:
    movq    $0, %rax
    leave
    ret
